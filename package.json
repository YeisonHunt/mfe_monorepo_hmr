{
    "name": "module-fed-monorepo",
    "version": "1.0.0",
    "private": true,
    "description": "Monorepo with HMR across micro-frontends",
    "scripts": {
        "dev": "turbo run dev",
        "build": "turbo run build",
        "clean": "turbo run clean && rm -rf node_modules"
    },
    "devDependencies": {
        "turbo": "^2.3.3"
    },
    "packageManager": "pnpm@9.0.0",
    "engines": {
        "node": ">=18"
    }
}